# in midterm_project
$ git reset --hard HEAD~

# in DataLad-101
$ cd ../
$ git reset --hard HEAD~
HEAD is now at 741bedd [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 24818bb add container and execute analysis within container

$ git log -p -2
commit a9a07aa6c45a898e21568e3945d4872c74c48229
Author: Adina Wagner <adina.wagner@t-online.de>
Date:   Mon Jul 15 09:24:33 2019 +0200

    add note on basic datalad run and datalad rerun

diff --git a/notes.txt b/notes.txt
index 540f757..35e6dc1 100644
--- a/notes.txt
+++ b/notes.txt
@@ -5,3 +5,11 @@ Note to self: Always use informative, concise commit messages.
 The command 'datalad install [--source] PATH' installs a dataset from e.g. a URL or a path.
 If you install a dataset into an existing dataset (as a subdataset), remember to specify the
 root of the superdataset with the '-d' option.
+The datalad run command can record the impact a script or command has on a Dataset.
+In its most simple form, datalad run only takes a commit message and the command that
+should be executed.
+
+Any datalad run command can be re-executed by using its commit checksum as an argument
+in datalad rerun CHECKSUM. DataLad will take information form the runrecord of the original
+commit, and re-execute it. If no changes happen with a rerun, the command will not be written
+to history. Note: you can also rerun a datalad rerun command!

commit f36836e748abdd1f1907556d37a753724da68b58
Author: Adina Wagner <adina.wagner@t-online.de>
Date:   Mon Jul 15 09:24:32 2019 +0200

    BF: list both directories content

diff --git a/code/list_titles.py b/code/list_titles.py
index d0c3416..5f5c617 100644
--- a/code/list_titles.py
+++ b/code/list_titles.py
@@ -6,7 +6,7 @@ from glob import glob
 # pointing from the root of DataLad-101 to the podcasts:
 
 path = join('recordings', 'longnow',
-            'Long_Now__Seminars_About_Long_term_Thinking', '*')
+            '*', '*')        # <-- here is the change!
 # create a list of all file names found under the path
 files = glob(path)
 # define an outfile name:

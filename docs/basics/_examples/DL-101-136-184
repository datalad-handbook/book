$ git reset --hard HEAD~
$ cd ../
$ git reset --hard HEAD~
HEAD is now at c810800 [DATALAD RUNCMD] rerun analysis in container
HEAD is now at cca3b0b add container and execute analysis within container

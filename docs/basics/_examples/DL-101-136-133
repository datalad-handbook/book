$ git reset --hard HEAD~
$ cd ../
$ git reset --hard HEAD~
HEAD is now at 9f56a5f [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 5a576b2 add container and execute analysis within container

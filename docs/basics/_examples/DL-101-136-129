# in midterm_project
$ git reset --hard HEAD~

# in DataLad-101
$ cd ../
$ git reset --hard HEAD~
HEAD is now at b700de3 [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 8556c89 add container and execute analysis within container

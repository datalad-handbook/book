$ git reset --hard HEAD~
$ cd ../
$ git reset --hard HEAD~
HEAD is now at 264f46e [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 88db2a0 add container and execute analysis within container

language: python
python:
  - "3.6"
install:
  - sudo apt-get install texlive-latex-recommended texlive-fonts-recommended texlive-latex-extra latexmk librsvg2-bin
  - pip install -r requirements.txt
  - pip install -e .
script:
  - make doctest
  - make html
  - make latexpdf

# in midterm_project
$ git reset --hard HEAD~

# in DataLad-101
$ cd ../
$ git reset --hard HEAD~
HEAD is now at e231e46 [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 1fae7c2 add container and execute analysis within container

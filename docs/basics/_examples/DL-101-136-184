$ git reset --hard HEAD~
$ cd ../
$ git reset --hard HEAD~
HEAD is now at 9aaf66a [DATALAD RUNCMD] rerun analysis in container
HEAD is now at 54c8935 add container and execute analysis within container

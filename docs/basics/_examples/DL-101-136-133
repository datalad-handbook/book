$ git reset --hard HEAD~
$ cd ../
$ git reset --hard HEAD~
HEAD is now at cc616ed [DATALAD RUNCMD] rerun analysis in container
HEAD is now at d5916e5 add container and execute analysis within container
